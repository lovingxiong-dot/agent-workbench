# v6.12 Task List — PresentationModel + Task 闭环

> **主线目标**：让 Workbench 从「有架构的框架」变成「可用的产品」。
>
> v6.11 完成了 Metadata Contract 与 Provider Registry；v6.12 的重点是：
> 1. 用 PresentationModel 彻底隔离 UI 与 Runtime。
> 2. 跑通第一条真实任务闭环（User → Manager → Planning → Capability → Provider → Streaming → Task → Trace → History）。
> 3. 通过真实使用验证架构稳定性，而不是继续增加抽象层。

---

## 全局约束

- **Metadata Contract 已冻结**（见 `PROJECT_BLUEPRINT.md`）：只允许 Additive Change，禁止 Breaking Change。
- **不再新增 Provider**：当前 7 个内置 Provider 已足够验证 Registry；未来 Provider 必须做到「零代码改动」接入。
- **UI 不拥有 Runtime / Provider / Resource**：UI 只通过 `PresentationModel` 读取，通过事件反向通信。

---

## Commit 6：Metadata → PresentationModel（v6.12.0-alpha.1）

### 目标
完成 `MetadataDefinition` 到 `PresentationModel` 的完整、单向、可测试映射，证明「任何 Metadata 都可以被描述并渲染到 Qt」。

### 文件规划
- `agent_workbench/ui/workbench/presentation.py` — 补全 `PresentationModel` 类型体系。
- `agent_workbench/ui/workbench/metadata_adapter.py` — 实现完整映射；可选手动命名为 `PresentationMetadataAdapter` 以区分 Protocol。
- `agent_workbench/metadata/adapter.py` — 确认 `MetadataAdapter` Protocol 不变（冻结）。
- `tests/ui/test_metadata_presentation.py` — 新增非 GUI 单元测试，覆盖所有字段映射。

### 任务
- [ ] 扩展 `PropertyPresentation`：保留 `default_value`、`category`、`sensitive`、`placeholder`、`required` 等字段。
- [ ] 扩展 `ActionPresentation`：保留 `enabled`、`order`、`danger`（破坏性操作标记）。
- [ ] 扩展 `ModulePresentation`：保留 `tags`、`enabled`、`order`、`category`。支持嵌套 `children`（用于 Resource 树）。
- [ ] 新增 `ResourcePresentation`（或复用 `ModulePresentation.children`），让 `ResourceDefinition` 也能进入 PresentationModel。
- [ ] 实现 `PresentationMetadataAdapter.adapt()` 完整映射，包括：
  - `MetadataDefinition → ModulePresentation`
  - `MetadataProperty → PropertyPresentation`
  - `MetadataStatistics → StatisticPresentation`
  - `MetadataAction → ActionPresentation`
  - 兼容 Legacy `ModuleMetadata` / `PropertyMetadata` / `StatisticMetadata` / `ActionMetadata`（保留至 v6.13.x 后移除）。
- [ ] `PresentationMetadataAdapter` 显式实现 `agent_workbench.metadata.adapter.MetadataAdapter` Protocol（可选 `Protocol` 标记）。
- [ ] 新增/更新非 GUI 单元测试，确保任意 `MetadataDefinition` 都能映射为 `ModulePresentation`，且不丢失字段。

### 验收标准
- [ ] 任意 Runtime Module 的 `metadata()` 输出经过 Adapter 后，得到完整 `ModulePresentation`。
- [ ] 新增字段通过 Additive Change 自动进入 PresentationModel，无需改 Adapter 逻辑。
- [ ] 单元测试覆盖率 100%（Adapter 所有分支）。
- [ ] `pytest` 全量通过。

### 附属小功能：Conversation 自动标题（v6.12.0-alpha.2）

> 用户感知强，但实现必须放在 Conversation Domain，而不是 UI 层。

#### 目标
模拟 ChatGPT 体验：新会话默认显示 `New Conversation`，第一轮 Assistant 回复完成后，异步生成标题并更新左侧 Navigator，全程不阻塞 Streaming。

#### 文件规划
- `agent_workbench/conversation/title_generator.py` — 标题生成策略接口与规则实现。
- `agent_workbench/conversation/title_service.py` — `ConversationTitleService`，协调生成与保存。
- `agent_workbench/conversation/manager.py`（或现有会话管理层）— 在 Assistant 第一轮回复完成后触发标题生成。
- `agent_workbench/conversation/store.py`（或现有 Store）— 保存 `Conversation.title` 与 `title_source`。

#### 任务
- [ ] 创建 `TitleGenerator` 协议 / 抽象类：
  - `RuleTitleGenerator`：取用户第一句话前 N 字或按关键词截断。
  - 预留 `LLMTitleGenerator`：调用模型总结（后续接入）。
  - 预留 `HybridTitleGenerator`：规则失败再调用 LLM。
- [ ] 创建 `ConversationTitleService.generate_title(conversation)`，异步执行，不阻塞主线程。
- [ ] 会话创建时默认 `title=""`，Navigator 渲染为 `New Conversation`。
- [ ] Assistant 第一轮回复完成后，若 `title == ""` 且未被用户手动修改（`title_source != "user"`），触发标题生成。
- [ ] 生成后更新 `Conversation.title` → `ConversationStore.update()` → `EventBus` → Navigator 自动刷新。
- [ ] 用户手动重命名后设置 `title_source="user"`/`title_locked=True`，自动标题不再覆盖。

#### 验收标准
- [ ] 新建会话左侧显示 `New Conversation`。
- [ ] 第一轮 Assistant 回复完成后，左侧标题自动变为生成的短标题（如 `Python 文件分析`）。
- [ ] 标题生成过程不影响 Streaming 输出。
- [ ] 用户手动修改标题后，自动标题不再覆盖。
- [ ] `pytest` 全量通过。

---

## Commit 7：Navigator + Inspector 读取 PresentationModel（v6.12.0-alpha.3）

### 目标
左侧 Navigator 与右侧 Inspector 不再按模块类型硬编码，全部读取 `ModulePresentation`。

### 文件规划
- `agent_workbench/ui/workbench/navigator.py`
- `agent_workbench/ui/workbench/inspector.py`
- `agent_workbench/ui/workbench_ui_controller.py`
- `tests/ui/test_navigator_inspector_presentation.py`

### 任务
- [x] `WorkbenchUIController._refresh_navigator()` 从 `list[ModulePresentation]` 渲染，移除硬编码 tab 注册。
- [x] `Navigator` 支持分组（按 `category` / `type`）和图标（`icon`）。
- [x] `Inspector.set_object()` 完整消费 `ModulePresentation`：
  - 按 `category` 分组显示 Properties。
  - 识别 `sensitive=True` 的 Property 使用密码输入框。
  - 识别 `editable=False` 的 Property 使用只读显示。
  - Actions 根据 `enabled` 禁用/启用按钮。
- [x] 选中 Navigator 项时，Inspector 自动重新渲染对应 `ModulePresentation`。
- [x] 新增非 GUI 单元测试：给定 `ModulePresentation`，验证 Navigator 模型数据与 Inspector 字段数。

### 验收标准
- [x] 新增一个 Runtime Module 后，Workbench 启动即可在 Navigator 看到，无需修改 `navigator.py`。
- [x] Inspector 中字段类型（string / number / boolean / select / textarea / json / password）由 `PropertyPresentation.type` 决定。
- [x] `pytest` 全量通过。

---

## Commit 8：StatusBar + ToolBar + Workspace 聚合 PresentationModel（v6.12.0-beta.1）

### 目标
StatusBar、ToolBar、Workspace 统一从 `PresentationModel` 聚合状态，UI 最后一个硬编码入口被消除。

### 文件规划
- `agent_workbench/ui/workbench/status_bar.py`
- `agent_workbench/ui/workbench/tool_bar.py`（如不存在则创建）
- `agent_workbench/ui/workbench_ui_controller.py`
- `tests/ui/test_status_bar_presentation.py`

### 任务
- [x] `StatusBar` 通过 `WorkbenchUIController` 聚合所有 `ModulePresentation.statistics`，显示当前 Provider、Runtime 状态、Token 统计等。
- [x] `ToolBar` 根据当前选中的 `ModulePresentation.actions` 动态生成快捷按钮。
- [x] `Workspace` 根据选中项类型切换视图（Chat / Trace / Config / Generic），不再硬编码模块 ID 分支。
- [x] 新增非 GUI 单元测试：验证 `StatusBar` 能从多个 `ModulePresentation` 聚合统计。

### 验收标准
- [x] StatusBar 不再直接读取 Runtime 或 Config，只读取 PresentationModel。
- [x] 新增 Module 的 statistic 自动出现在 StatusBar，无需修改 `status_bar.py`。
- [x] `pytest` 全量通过。

---

## Commit 8.5：ViewSchema 布局协议层（v6.12.0-beta.2）

### 目标
在 `PresentationModel` 与 `Qt Renderer` 之间增加一层 `ViewSchema` 布局协议，描述 Module 的 PresentationModel 应如何摆放在 Toolbar / Inspector / StatusBar / Workspace / Dock 中，使 UI 真正具备「数据驱动、可配置、跨端复用」的能力。

### 文件规划
- `agent_workbench/ui/workbench/view_schema.py`
- `agent_workbench/ui/workbench/view_schema_registry.py`
- `agent_workbench/ui/workbench/view_schema_renderer.py`
- `agent_workbench/ui/workbench/inspector.py`
- `agent_workbench/ui/workbench_ui_controller.py`
- `tests/ui/test_view_schema.py`

### 任务
- [x] 定义 `ViewSchema` 协议层：`ToolbarSchema`、`InspectorSchema`（Tab + category 过滤）、`StatusSchema`、`DockSchema`、`WorkspaceSchema`。
- [x] 实现 `ViewSchemaRegistry`，注册 7 个默认布局协议，支持按 `view_schema_id` 或 `type` 解析。
- [x] 实现 `ViewSchemaRenderer`（Qt），将 Schema 应用到 `ToolBar / StatusBar / Inspector / Workspace`。
- [x] `ModulePresentation` 增加 `view_schema_id`，`MetadataAdapter` 自动推断默认值并允许 Metadata 显式覆盖。
- [x] `Inspector` 支持 Schema 驱动 Tab 模式；无 Schema 时保持原有平铺模式，向后兼容。
- [x] `WorkbenchUIController._on_selection_changed()` 改为通过 `ViewSchemaRenderer` 统一驱动所有 UI Host。
- [x] 新增非 GUI 单元测试覆盖 Registry 解析、Renderer 驱动、MetadataAdapter 推断。

### 验收标准
- [x] 新增一个 Capability 后，可通过配置 `view_schema_id` 自动获得对应 Workbench 布局，无需修改 Qt 代码。
- [x] `Inspector` 能按 Schema Tab 过滤并分组显示 Properties。
- [x] `ToolBar` 能按 Schema 分组/排序显示 Actions。
- [x] `StatusBar` 能按 Schema 选择显示 Runtime 状态与 Module Statistics。
- [x] `pytest` 全量通过。

---

## Commit 9：Dynamic UI Binding Layer（v6.12.0-beta.3）

### 目标
在 ViewSchema 与 Qt Renderer 之间建立 Dynamic UI Binding Layer，实现 `Metadata → PresentationModel → ViewSchema → BindingContext → Qt Renderer` 的完整数据流。Runtime 状态通过声明式 `BindingSource` 路径注入 UI，Renderer 不再直接访问 Runtime。

### 文件规划
- `agent_workbench/ui/workbench/binding_context.py` — BindingProvider / BindingRegistry / BindingContext。
- `agent_workbench/ui/workbench/view_schema.py` — `BindingSource`、`PropertyBinding`、`StatusItemSchema.binding`。
- `agent_workbench/ui/workbench/view_schema_renderer.py` — 集成 BindingContext，解析 StatusBar / Inspector 动态值。
- `agent_workbench/ui/workbench/presentation.py` — `PropertyPresentation.binding`。
- `agent_workbench/ui/workbench/view_schema_registry.py` — 默认 Schema 使用 BindingSource 引用 Runtime 状态。
- `agent_workbench/ui/workbench_ui_controller.py` — 注册 `runtime` Provider，驱动 Renderer。
- `tests/ui/test_binding_context.py` — Binding Layer 非 GUI 单元测试。

### 任务
- [x] 定义 `BindingSource` / `PropertyBinding`，支持 `path` + `format`。
- [x] 实现 `BindingProvider` / `BindingRegistry` / `BindingContext` 路径解析。
- [x] `ViewSchemaRenderer` 构造时接收 `BindingContext`。
- [x] StatusBar 项支持 `source="binding"`，通过 `BindingContext.resolve()` 取值。
- [x] Inspector 只读 Property 支持 `binding`，渲染时注入 Runtime 当前值。
- [x] `WorkbenchUIController` 注册 `runtime` Provider，移除旧的 `runtime_status=` 渲染参数。
- [x] 新增 `tests/ui/test_binding_context.py` 非 GUI 单元测试。
- [x] 更新 `tests/ui/test_view_schema.py` 以适配 BindingContext。
- [x] 修复 `StatusBarHost.set_statistics()` 接口缺失。

### 验收标准
- [x] Runtime 状态变化后，StatusBar / Inspector 动态绑定值可刷新。
- [x] 新增状态项只需在 ViewSchema 中声明 `BindingSource`，无需改 Renderer。
- [x] `pytest` 全量通过（741/741）。
- [x] 文档同步更新（CHANGELOG、PROJECT_BLUEPRINT、本清单）。

---

## Commit 10：ViewComponentRegistry 契约层（v6.12.0-beta.4）

> **核心原则**：Registry 只负责「找得到」，不负责「做出来」。不要把 Terminal / Editor / Chart 等复杂控件塞进来，那些是 **Builtin Component**。

### 目标
为 View 层定义一个最小化的组件注册与查找契约，让 Renderer 从直接渲染具体区域（`render_status_bar`）过渡到按组件 ID 查找并调度（`render(component_id)`）。

### 文件规划
- `agent_workbench/ui/workbench/view_component.py` — `ViewComponentDefinition`。
- `agent_workbench/ui/workbench/view_component_registry.py` — `ViewComponentRegistry`。
- `agent_workbench/ui/workbench/view_schema_renderer.py` — 改为 `render(component_id)` 入口。
- `tests/ui/test_view_component_registry.py` — 非 GUI 单元测试。

### 允许做（仅限三件）
- [x] 定义 `ViewComponentDefinition`：
  - `id: str`
  - `renderer: str`（renderer 标识，不实例化 Qt）
  - `supported_schema: list[str]`（可处理的 schema 类型）
  - `supported_binding: list[str]`（可消费的 binding namespace）
  - `default_size: dict | None`（如 `{"width": 400, "height": 300}`）
  - **不要更多字段**。
- [x] 定义 `ViewComponentRegistry`：
  - `register(definition: ViewComponentDefinition)`
  - `unregister(component_id: str)`
  - `resolve(component_id: str) -> ViewComponentDefinition | None`
  - `list() -> list[ViewComponentDefinition]`
  - **只有四个接口**。
- [x] `ViewSchemaRenderer` 改为 `render(component_id: str)` 入口：
  - 内部按 component_id 找到 `ViewComponentDefinition`。
  - 再委托到现有 `_render_toolbar` / `_render_status_bar` / `_render_inspector` / `_render_workspace`。
  - 这一步只做接口收敛，不新增任何组件实现。

### 明确禁止
- [x] ❌ Terminal
- [x] ❌ Editor / Monaco
- [x] ❌ Chart / Graph
- [x] ❌ Dock / Tree / Split
- [x] ❌ 任何具体 Qt 控件实现
- [x] ❌ 把 Registry 当成 Component 工厂

### 验收标准
- [x] `ViewComponentRegistry` 四个接口全部有非 GUI 单元测试。
- [x] `ViewSchemaRenderer.render(component_id)` 能正确分发到现有四个区域渲染。
- [x] `pytest` 全量通过（747/747）。
- [x] 文档同步更新。

---

## Commit 11：跳过

> **Layout Persistence 不在 v6.12 做**。用户已确认跳过。

---

## Commit 12：Agent Package + Echo Agent（v6.12.0-beta.5）

> **分水岭目标**：Agent 第一次成为「可放置、可发现、可删除」的对象，而不是代码。做到：
> ```text
> 删掉 packages/echo_agent  → Workbench 中 Echo Agent 消失
> 复制 packages/echo_agent  → Workbench 中 Echo Agent 自动出现
> ```

### 目标
让 Agent Workbench 能够扫描一个外部 `packages/` 目录，自动发现并加载其中的 Agent Package，无需修改 Workbench 源码。

### 文件规划
- `agent_workbench/package/` — Package 发现、加载、生命周期管理。
- `packages/echo_agent/` — 第一个示例 Agent Package（Echo Agent）。
- `agent_workbench/ui/workbench/agent_workspace.py` — Agent 选中后的动态 Workspace（可选， Minimal Viable 可复用 generic workspace）。
- `scripts/acceptance_test_agent_package.py` — 人工/半自动验收脚本。
- `tests/package/test_package_discovery.py` — Package 发现与加载的单元测试。

### 任务
- [ ] 定义 `AgentPackageDefinition`：
  - `id`, `name`, `version`, `entrypoint`, `metadata_path`, `icon`, `description`
  - 包含 `metadata.yaml` 或 `metadata.json` 的读取规范。
- [ ] 实现 `PackageRegistry`：
  - 扫描指定 `packages/` 目录。
  - 加载/卸载 Package。
  - 向 Workbench 注册对应的 `MetadataDefinition`。
- [ ] 创建 `packages/echo_agent/`：
  - `metadata.yaml`：声明 Echo Agent 的 Capability、Actions、Bindings。
  - `echo_agent.py`：Echo Provider / Skill 实现（零代码改动接入）。
  - 资源文件（icon 等）。
- [ ] `WorkbenchUIController` 启动时扫描 `packages/`，将发现的 Agent 加入 Navigator。
- [ ] 支持运行时刷新（新增/删除 Package 后重新扫描）。

### 人工验收脚本
创建 `scripts/acceptance_test_agent_package.py`，执行以下步骤并输出 PASS/FAIL：

```text
① 删除 packages/echo_agent
   → Workbench 无异常启动
   → Navigator 中无 Echo Agent

② 复制 packages/echo_agent
   → Workbench 自动发现
   → Navigator 中出现 Echo Agent

③ 点击 Echo Agent
   → Workspace 自动生成/切换
   → Inspector 显示 Agent 属性

④ 点击 Action（如 Run）
   → Binding 自动刷新
   → StatusBar 显示执行状态

⑤ 删除 Package
   → Workbench 自动消失
   → Navigator 中无 Echo Agent
```

### 验收标准
- [ ] `packages/echo_agent` 可以独立存在，不依赖 Workbench 内部代码。
- [ ] 删除/复制 Package 后，Workbench 行为符合验收脚本 5 步。
- [ ] `pytest` 全量通过（含 Package 发现单元测试）。
- [ ] 验收脚本输出 PASS。
- [ ] 文档同步更新。

---

## v6.12 优先级（用户确认版）

1. **PresentationModel 完整打通**（Commit 6/7/8）—— 彻底去掉 UI 硬编码。
2. **Task 闭环**（Commit 9）—— 验证架构在真实使用中的稳定性。
3. **真实使用一段时间** —— 通过实际使用发现 Runtime、Metadata、UI 的问题。
4. **最后**再考虑 Marketplace、Plugin、Remote、Multi-Agent 等更大的扩展。

---

## 现在不做清单（v6.12 期间）

- **新增 Provider**：Registry 已足够，后续 Provider 必须零代码接入。
- **新增 Runtime 类型**：Knowledge / Persona / Browser / Plugin / Gateway / Digital Identity 不接。
- **Schema-driven UI**：v6.12 仍手动映射 PresentationModel，v6.13.x 再考虑从 Schema 自动生成控件。
- **Runtime Executors**：ProviderRuntime / ToolRuntime / SkillRuntime 放到 Schema Foundation 之后。
- **复杂 Workflow DAG**：只做基础串行任务流 A → B → C，且放到更晚阶段。
- **Multi-Agent / Marketplace / Remote / Web Plugin**：在 Task 闭环稳定前不做。

---

## 全局验收标准

每个 Commit 完成后必须满足：

1. **所有新增代码有非 GUI 单元测试**。
2. **Workbench 能稳定启动并演示该阶段功能**。
3. **`pytest` 全量通过**。
4. **`python -B scripts/audit_repository.py` 100/100**。
5. **文档同步更新**（本清单、ROADMAP、PROJECT_BLUEPRINT.md、数据层清单）。
6. **每阶段能回答**：Workbench 今天比昨天多支持一种什么类型的对象，而无需修改代码？
