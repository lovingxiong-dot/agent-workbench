# v6.11.x 可执行任务清单：Metadata-driven Workbench

> **目标**：让所有已支持对象具备统一的 Metadata 描述能力。
>
> **每阶段原则**：Workbench 今天比昨天多支持一种什么类型的对象，而无需修改代码？
>
> **核心规则**：UI 永远只认识 `PresentationModel`，不直接调用 Service；`BaseRuntimeModule.metadata()` 返回平台无关的 Capability Metadata。
>
> **阶段边界**：
> - v6.11.x：Metadata-driven（统一描述对象）
> - v6.12.x：Schema-driven（统一描述配置，自动生成 UI）
> - v6.13.x：Plugin-driven（零代码扩展）

---

## Commit 1：Metadata Contract & Base Model（v6.11.0-alpha）

目标：冻结 `agent_workbench/metadata/` 目录与 `MetadataDefinition` 契约，让 Runtime、Workbench、Plugin、CLI、Web 共享同一套对象描述语言。

### 文件规划（目录一次性冻结）

```text
agent_workbench/metadata/
    __init__.py
    types.py       # 公共类型别名与枚举（ValueType, MetadataType 等）
    errors.py      # Metadata 相关异常
    model.py       # MetadataDefinition / MetadataProperty / MetadataAction / MetadataStatistics
    registry.py    # MetadataRegistry：register / get / all
    adapter.py     # MetadataAdapter：空实现或基础实现，为 v6.12.x Plugin Metadata 预留
```

### 核心对象

#### `MetadataDefinition`
静态描述一个 Runtime 对象。

- `id: str`
- `type: str`（`provider`, `mcp`, `skill`, `workflow`, `prompt`, `memory` 等）
- `name: str`
- `description: str`
- `icon: str`（平台无关标识，如 `"provider"`, `"mcp"`, `"skill"`）
- `properties: list[MetadataProperty]`
- `statistics: list[MetadataStatistics]`
- `actions: list[MetadataAction]`
- `tags: list[str]`
- `enabled: bool`

#### `MetadataProperty`
描述对象的可配置属性。例如 `temperature`, `api_key`, `endpoint`, `enabled`。

- `id: str`
- `name: str`
- `description: str`
- `value_type: ValueType`（string / int / float / bool / list / dict / secret / path / enum）
- `current_value: Any`
- `default_value: Any`
- `options: list[Any]`（可选值列表，用于 enum / select）
- `editable: bool`
- `sensitive: bool`（标记 secret / api_key，但不指定具体 UI 控件）
- `category: str`

#### `MetadataAction`
描述对象可触发的操作。例如 `Connect`, `Disconnect`, `Refresh`, `Install`, `Remove`。

- `id: str`
- `label: str`
- `description: str`
- `icon: str`
- `enabled: bool`

#### `MetadataStatistics`
描述对象的运行时统计。例如 `tokens`, `latency`, `memory_usage`, `version`。

- `id: str`
- `name: str`
- `value: Any`
- `unit: str`
- `timestamp: datetime | None`

### 任务
- [ ] 创建 `agent_workbench/metadata/` 目录与上述五个文件。
- [ ] 在 `types.py` 中定义 `ValueType` 枚举与 `MetadataType` 类型别名。
- [ ] 在 `errors.py` 中定义 `MetadataError`, `MetadataValidationError`, `MetadataNotFoundError`。
- [ ] 在 `model.py` 中实现四个核心类，全部使用 `dataclass` 或 Pydantic-like 纯数据模型。
- [ ] 在 `registry.py` 中实现 `MetadataRegistry`：
  - `register(definition: MetadataDefinition)`
  - `get(definition_id: str) -> MetadataDefinition | None`
  - `all() -> list[MetadataDefinition]`
- [ ] 在 `adapter.py` 中创建 `MetadataAdapter` 骨架：
  - `adapt(definition: MetadataDefinition) -> ModulePresentation`
  - 当前可为空实现或基础映射实现。
- [ ] 更新 `agent_workbench/runtime/modules/base.py`：
  - `BaseRuntimeModule.metadata()` 返回 `MetadataDefinition`。
  - 提供默认实现，返回 `id=self.module_id()`, `type=self.module_type()`, `name=...`, `properties=[], statistics=[], actions=[]`。
- [ ] 创建 `tests/metadata/test_metadata_model.py`, `test_metadata_registry.py`, `test_metadata_adapter.py`。
- [ ] 确保 `agent_workbench/metadata/` 不导入任何 Runtime 内部模块（只使用基础类型）。

### 验收标准
- [ ] `agent_workbench/metadata/` 目录结构冻结，后续不再改名或迁移。
- [ ] `MetadataDefinition` 可在 Qt / Web / CLI / REST / Plugin 五种场景下被消费。
- [ ] `MetadataRegistry.register / get / all` 通过非 GUI 测试。
- [ ] `BaseRuntimeModule.metadata()` 子类实现返回严格类型化的 `MetadataDefinition`。
- [ ] `pytest` 全量通过。

### 设计约束
- **Metadata 永远不能依赖 Runtime**。只允许 `Runtime → Metadata`。
- **Metadata 不包含 UI 概念**。不出现 `widget`, `editor`, `layout`, `qt_class` 等字段。
- **目录冻结**。即使 `registry.py` / `adapter.py` 当前为空实现，也必须存在于 `agent_workbench/metadata/`。

---

## Commit 2：MetadataAdapter & PresentationModel（v6.11.0-alpha）

目标：建立 `MetadataDefinition → PresentationModel` 的翻译层，让 UI 只依赖 PresentationModel。

### 文件规划
- `agent_workbench/ui/presentation/__init__.py`
- `agent_workbench/ui/presentation/model.py` — `ModulePresentation`, `PropertyPresentation`, `StatisticPresentation`, `ActionPresentation`
- `agent_workbench/metadata/adapter.py` — `MetadataAdapter`（跨层适配器，由 Metadata 契约拥有）

### 任务
- [ ] 定义 `ModulePresentation`：
  - `id`, `type`, `name`, `description`, `icon`
  - `properties: list[PropertyPresentation]`
  - `statistics: list[StatisticPresentation]`
  - `actions: list[ActionPresentation]`
- [ ] 定义 `PropertyPresentation`：
  - 对应 `MetadataProperty`，增加 UI 无关的渲染提示（如 `sensitive` 标记 secret，但不指定具体控件）
- [ ] 定义 `StatisticPresentation`
- [ ] 定义 `ActionPresentation`
- [ ] 实现 `MetadataAdapter.adapt(definition: MetadataDefinition) -> ModulePresentation`
- [ ] 确保 Metadata → Presentation 是一一映射，不引入业务判断。
- [ ] 非 GUI 单元测试覆盖 Adapter 所有路径。

### 验收标准
- [ ] Navigator 可通过 `ModulePresentation` 渲染模块列表。
- [ ] Inspector 可通过 `ModulePresentation` 渲染属性、统计、操作。
- [ ] `pytest` 全量通过。

---

## Commit 3：Runtime Module Metadata 补齐（v6.11.0-alpha）

目标：Provider / MCP / Skill / Workflow / Prompt / Memory 全部返回符合 Contract 的 Metadata。

### 文件规划
- `agent_workbench/runtime/modules/model_module.py`
- `agent_workbench/runtime/modules/mcp_module.py`
- `agent_workbench/runtime/modules/skill_module.py`
- `agent_workbench/runtime/modules/workflow_module.py`
- `agent_workbench/runtime/modules/prompt_module.py`
- `agent_workbench/runtime/modules/memory_module.py`

### 任务
- [ ] `ModelModule.metadata()` 返回当前已注册 Provider 列表作为 `properties` 或子对象引用。
- [ ] `McpModule.metadata()` 返回 MCP Server 列表、状态、连接统计。
- [ ] `SkillModule.metadata()` 返回 Skill Registry 内容。
- [ ] `WorkflowModule.metadata()` 返回 Workflow Template 列表。
- [ ] `PromptModule.metadata()` 返回 Prompt 列表与引用统计。
- [ ] `MemoryModule.metadata()` 返回 Memory Store 列表、容量、命中统计。
- [ ] 每个模块的 `metadata()` 不直接读取 UI 状态，只读取 Runtime 内部状态。
- [ ] 每个模块的 `metadata()` 通过非 GUI 测试验证。

### 验收标准
- [ ] 6 个对象类型的 Metadata 结构一致，可通过同一套 Adapter 消费。
- [ ] `pytest` 全量通过。

---

## Commit 4：Navigator 去硬编码（v6.11.0-alpha）

目标：Navigator 不再按类型维护私有映射，全部通过 `PresentationModel` 渲染。

### 文件规划
- `agent_workbench/ui/workbench/navigator.py`
- `agent_workbench/ui/workbench/navigator_host.py`
- `agent_workbench/ui/workbench_ui_controller.py`

### 任务
- [ ] Navigator 接收 `list[ModulePresentation]` 作为输入。
- [ ] 根据 `ModulePresentation.type` 决定图标（通过 icon 字段映射，而非硬编码 if/else）。
- [ ] Settings 分类下的实例列表同样来自 `ModulePresentation.properties` 或子 Presentation。
- [ ] 点击模块/实例时，发出 `selection_changed(module_id, object_id)` 信号。
- [ ] 删除所有针对 Provider / MCP / Skill / Workflow / Prompt / Memory 的硬编码分支。
- [ ] 非 GUI 单元测试覆盖 Navigator 渲染逻辑。

### 验收标准
- [ ] 新增一个 Runtime Module 后，Navigator 自动出现对应条目，无需修改 Navigator 代码。
- [ ] `pytest` 全量通过。

---

## Commit 5：Inspector 去硬编码（v6.11.0-alpha）

目标：Inspector 根据 `PresentationModel` 动态渲染属性、统计、操作。

### 文件规划
- `agent_workbench/ui/workbench/inspector.py`
- `agent_workbench/ui/workbench/inspector_host.py`
- `agent_workbench/ui/workbench/metadata_adapter.py`（如需要增强）

### 任务
- [ ] Inspector 接收 `ModulePresentation` 作为输入。
- [ ] 根据 `PropertyPresentation` 的 `value_type` 和 `editable` 渲染对应编辑器（当前阶段仍允许手写控件映射）。
- [ ] 显示 `StatisticPresentation` 为只读统计区。
- [ ] 显示 `ActionPresentation` 为可点击操作按钮。
- [ ] 属性变更时，发出 `property_changed(module_id, object_id, property_id, value)` 信号，由 Controller 转发给 Runtime。
- [ ] 删除所有针对具体模块类型的硬编码 Inspector 面板。
- [ ] 非 GUI 单元测试覆盖 Inspector 渲染与信号。

### 验收标准
- [ ] 选择一个 Provider 和选择一个 Skill，Inspector 使用同一套渲染路径。
- [ ] `pytest` 全量通过。

---

## Commit 6：StatusBar 去硬编码（v6.11.0-alpha）

目标：StatusBar 通过 `PresentationModel` 读取统计信息，不再直接访问 Runtime Module。

### 文件规划
- `agent_workbench/ui/workbench/status_bar.py`
- `agent_workbench/ui/workbench/status_bar_host.py`

### 任务
- [ ] StatusBar 订阅 `WorkbenchUIController` 聚合后的 `ModulePresentation` 列表。
- [ ] 从每个 `ModulePresentation.statistics` 中提取关键指标显示。
- [ ] 删除所有针对具体模块类型的硬编码统计读取。
- [ ] 非 GUI 单元测试覆盖 StatusBar 统计聚合。

### 验收标准
- [ ] Memory 模块新增一个 Store 后，StatusBar 统计自动更新，无需修改 StatusBar 代码。
- [ ] `pytest` 全量通过。

---

## 全局验收标准

每个 Commit 完成后必须满足：

1. **所有新增代码有非 GUI 单元测试**。
2. **Workbench 能稳定启动并演示该阶段功能**。
3. **`pytest` 全量通过**。
4. **`python -B scripts/audit_repository.py` 100/100**。
5. **文档同步更新**（本清单、ROADMAP、PROJECT_BLUEPRINT.md、数据层清单）。
6. **每阶段能回答**：Workbench 今天比昨天多支持一种什么类型的对象，而无需修改代码？

---

## 现在不做清单

- **Schema-driven UI**：不自动生成控件，v6.12.x 再做。
- **Runtime Executors**：ProviderRuntime / ToolRuntime / SkillRuntime 放到 Schema Foundation 之后。
- **新增 Runtime 类型**：Knowledge / Persona / Browser / Plugin / Gateway / Digital Identity 不接。
- **真实 Provider 接入**：Claude / Gemini / OpenAI / Kimi / Qwen / DeepSeek 不接。
- **复杂 Workflow DAG**：只做基础串行任务流 A → B → C，且放到更晚阶段。
