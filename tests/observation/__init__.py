"""tests/observation/ — Phase 3.12 Observation Layer Contract Schema Tests.

Phase 3.12 Batch 1: Observation Contract Schema tests (Schema-First).

测试层级：
- tests/observation/contract/ → ObservationArtifact / ObservationType / ObservationScore
- (Future) tests/observation/adapters/ → Frozen Input Adapters
- (Future) tests/observation/registry/ → Observation Registry

注：tools/observation/ 下已有 prototype (adapters, derived, reports, collectors) tests at tests/tools/observation/。
本目录 tests/observation/ 是 Phase 3.12 Frozen Contract Schema 专用测试。
"""
